# acp (Python)

Python 3.11+ reference SDK for the Aurora Communications Protocol.

This package is the reference encoder for ACP-CDE-1.2 golden vectors and the home of `acp-inspect` / `acp-sim`.

```bash
python3 -m pip install -e '.[dev]'
python3 -m pytest
```
